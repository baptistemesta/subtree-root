plugins{
    id "org.ajoberstar.grgit" version "2.0.1"
}

repositories {
    mavenLocal()
    mavenCentral()
    jcenter()
}

task subtreePush(type:Exec){
    commandLine 'git', 'subtree', 'push', '--prefix', 'sub-project', 'git@github.com:baptistemesta/subtree-child.git',
    'master'
}

task subtreePull{

}